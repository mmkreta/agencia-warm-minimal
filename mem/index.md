# Project Memory

## Core
Default theme is ALWAYS dark (black) on every load/refresh. Light mode is opt-in only via toggle. Never change the default.

## Memories
- [Theme default](mem://preferences/theme-default) — Dark is default, light is optional
